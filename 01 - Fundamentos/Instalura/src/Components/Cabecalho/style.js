import {StyleSheet} from 'react-native'

const style = StyleSheet.create({
    fotoUsuario:{
        width: 40,
        height: 40,
        margin: 10,
        borderRadius:5,
    },
    cabecalho:{
        flexDirection:"row",
        alignItems:"center",

    },
})

export default style;